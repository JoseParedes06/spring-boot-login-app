spring.application.name=login-app

# Base de datos
spring.datasource.url=jdbc:mysql://localhost:3306/login_app?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Jochi1906o!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate — crea/actualiza la tabla automaticamente
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Thymeleaf — desactiva cache en desarrollo
spring.thymeleaf.cache=false
